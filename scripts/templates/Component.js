// External imports
import React from 'react'
import PropTypes from 'prop-types'

// Internal imports
import './Styles.scss'

const <%= name %> = () => {

    return (
        <div>
            <%= name %> works!
        </div>
    )
}

<%= name %>.propTypes = {
        
}


export default <%= name %>